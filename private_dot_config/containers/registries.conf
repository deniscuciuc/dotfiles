# Docker-style short image names resolve to Docker Hub without a TTY prompt.
# Prefer fully qualified images in new projects.
unqualified-search-registries = ["docker.io"]
short-name-mode = "disabled"
